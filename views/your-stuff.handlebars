<section>
  <h2>Create a New Trade</h2>

  <form class="new-item-form">
    <div>
      <label for="item-title">Title</label>
      <input type="text" id="item-title" name="item-title" />
    </div>

    <div>
      <label for="description">Description</label>
      <input type="text" id="description" name="description" />
      {{! <textarea name="description" id="description"></textarea> }}
    </div>

    <div>
      <p><input
          type="file"
          accept="image/*"
          name="image"
          id="file"
          onchange="loadFile(event)"
          style="display: none;"
        /></p>
      <p><label for="file" style="cursor: pointer;">Upload Image</label></p>
      <p><img id="output" width="200" /></p>
    </div>
    <button type="submit" class="btn">Create</button>
  </form>
</section>

{{!-- {{#if items.length}}
<section>
  <h2>Your Stuff</h2>
  <ol>
    {{#each items as |item|}}
    <li>
      {{> item-info item}}
      <a href="/your-stuff/edit/{{item.id}}" class="edit-link">Edit post</a>
    </li>
    {{/each}}
  </ol>
</section>
{{/if}} --}}

{{!-- <script>
  var loadFile = function(event) { var image =
  document.getElementById('output'); image.src =
  URL.createObjectURL(event.target.files[0]); };
</script> --}}

<script src="/javascript/add-item.js"></script>